# 
# shortcuts to using docker and docker-compose
#

# docker aliases
alias dcu='docker-compose up --build'
alias dr='docker-compose down && docker-compose up --build'
alias dx='docker exec -ti'

# docker functions
dxm() {
  container=$1
  shift
  docker exec -ti $container bin/python manage.py $@
}

